diff --git a/scripts/tui_polish.patch b/scripts/tui_polish.patch
new file mode 100644
index 0000000000000000000000000000000000000000..767d8ddbd35ef9864a3e4e3c680e10a784443ae6
--- /dev/null
+++ b/scripts/tui_polish.patch
@@ -0,0 +1,263 @@
+diff --git a/perfodia.py b/perfodia.py
+index e43e2c2..76086ca 100644
+--- a/perfodia.py
++++ b/perfodia.py
+@@ -15,6 +15,7 @@ import argparse
+ import sys
+ import signal
+ import logging
++from collections import Counter
+ from datetime import datetime
+ from pathlib import Path
+ 
+@@ -406,6 +407,11 @@ def run_workflow(args, config, targets, exclusions, session_dir,
+     }
+ 
+     total_modules = len(module_list)
++    if tui_state:
++        for mk in module_list:
++            if mk in module_map:
++                phase_name, _ = module_map[mk]
++                tui_state.set_phase_status(phase_name, "queued")
+ 
+     for idx, module_key in enumerate(module_list, 1):
+         if module_key not in module_map:
+@@ -415,6 +421,9 @@ def run_workflow(args, config, targets, exclusions, session_dir,
+         # ── Resume: skip already-completed phases ──
+         if session_state and session_state.should_skip_phase(module_key):
+             logger.info(f"[RESUME] Skipping '{module_key}' (already completed)")
++            if tui_state:
++                phase_name, _ = module_map[module_key]
++                tui_state.set_phase_status(phase_name, "skipped")
+             continue
+ 
+         phase_name, module_class = module_map[module_key]
+@@ -430,6 +439,7 @@ def run_workflow(args, config, targets, exclusions, session_dir,
+                 phase_progress=idx,
+                 total_phases=total_modules,
+             )
++            tui_state.set_phase_status(phase_name, "running")
+ 
+         try:
+             module = module_class(
+@@ -454,10 +464,29 @@ def run_workflow(args, config, targets, exclusions, session_dir,
+                 if hosts:
+                     vuln_scorer.score_nmap_scripts(hosts)
+                     if tui_state:
++                        port_counter = Counter()
++                        host_port_counts = []
++                        for h in hosts:
++                            host_ports = h.get("ports", []) or []
++                            host_name = h.get("host", h.get("ip", "unknown"))
++                            host_port_counts.append((host_name, len(host_ports)))
++                            for p in host_ports:
++                                if isinstance(p, dict):
++                                    port_num = p.get("port")
++                                else:
++                                    port_num = p
++                                if port_num:
++                                    try:
++                                        port_counter[int(port_num)] += 1
++                                    except (TypeError, ValueError):
++                                        continue
++                        top_hosts = sorted(host_port_counts, key=lambda x: x[1], reverse=True)
++                        top_ports = port_counter.most_common(8)
+                         tui_state.update(
+                             hosts_found=len(hosts),
+                             ports_found=sum(len(h.get("ports", [])) for h in hosts),
+                         )
++                        tui_state.update_activity(top_hosts=top_hosts, top_ports=top_ports)
+ 
+             # ── Score exploit matches ──
+             if module_key == "exploit" and vuln_scorer:
+@@ -487,15 +516,24 @@ def run_workflow(args, config, targets, exclusions, session_dir,
+                 session_state.save_checkpoint(results, completed_phase=module_key)
+ 
+             logger.info(f"[+] {phase_name} phase completed successfully")
++            if tui_state:
++                tui_state.set_phase_status(phase_name, "done")
++                tui_state.set_last_success(f"{phase_name} phase completed")
+ 
+         except KeyboardInterrupt:
+             logger.warning(f"[!] {phase_name} phase interrupted by user")
++            if tui_state:
++                tui_state.set_phase_status(phase_name, "failed")
++                tui_state.set_last_warning(f"{phase_name} interrupted by user")
+             results["phases"][module_key] = {"status": "interrupted"}
+             if session_state:
+                 session_state.save_checkpoint(results, completed_phase=f"{module_key}_partial")
+             raise
+         except Exception as e:
+             logger.error(f"[!] {phase_name} phase failed: {e}", exc_info=True)
++            if tui_state:
++                tui_state.set_phase_status(phase_name, "failed")
++                tui_state.set_last_error(f"{phase_name} failed: {e}")
+             results["phases"][module_key] = {"status": "error", "error": str(e)}
+             if session_state:
+                 session_state.save_checkpoint(results, completed_phase=f"{module_key}_error")
+diff --git a/utils/tui.py b/utils/tui.py
+index daa9d51..de77960 100644
+--- a/utils/tui.py
++++ b/utils/tui.py
+@@ -13,7 +13,7 @@ from __future__ import annotations
+ import logging
+ import threading
+ import time
+-from typing import Dict, Any, Optional, List
++from typing import Dict, Any, Optional, List, Tuple
+ from datetime import datetime
+ 
+ logger = logging.getLogger(__name__)
+@@ -58,6 +58,12 @@ class DashboardState:
+         self.recent_events: List[str] = []
+         self.errors: int = 0
+         self.warnings: int = 0
++        self.last_error: str = ""
++        self.last_warning: str = ""
++        self.last_success: str = ""
++        self.phase_status: Dict[str, str] = {}
++        self.top_hosts: List[Tuple[str, int]] = []
++        self.top_ports: List[Tuple[int, int]] = []
+         self.start_time: datetime = datetime.now()
+         self.running: bool = True
+ 
+@@ -82,6 +88,29 @@ class DashboardState:
+             if len(self.findings) > 50:
+                 self.findings.pop(0)
+ 
++    def set_phase_status(self, phase: str, status: str) -> None:
++        """Track phase lifecycle status (queued/running/done/failed/skipped)."""
++        with self._lock:
++            self.phase_status[phase] = status
++
++    def set_last_error(self, message: str) -> None:
++        with self._lock:
++            self.last_error = message[:200]
++
++    def set_last_warning(self, message: str) -> None:
++        with self._lock:
++            self.last_warning = message[:200]
++
++    def set_last_success(self, message: str) -> None:
++        with self._lock:
++            self.last_success = message[:200]
++
++    def update_activity(self, top_hosts: List[Tuple[str, int]], top_ports: List[Tuple[int, int]]) -> None:
++        """Update derived scan activity tables."""
++        with self._lock:
++            self.top_hosts = top_hosts[:8]
++            self.top_ports = top_ports[:8]
++
+     def snapshot(self) -> Dict[str, Any]:
+         with self._lock:
+             return {
+@@ -99,6 +128,12 @@ class DashboardState:
+                 "recent_events": list(self.recent_events[-10:]),
+                 "errors": self.errors,
+                 "warnings": self.warnings,
++                "last_error": self.last_error,
++                "last_warning": self.last_warning,
++                "last_success": self.last_success,
++                "phase_status": dict(self.phase_status),
++                "top_hosts": list(self.top_hosts),
++                "top_ports": list(self.top_ports),
+                 "elapsed": (datetime.now() - self.start_time).total_seconds(),
+                 "running": self.running,
+             }
+@@ -172,10 +207,12 @@ class TUIDashboard:
+         )
+         layout["left"].split_column(
+             Layout(name="stats", size=7),
++            Layout(name="activity", size=10),
+             Layout(name="events"),
+         )
+         layout["right"].split_column(
+-            Layout(name="severity", size=12),
++            Layout(name="severity", size=11),
++            Layout(name="phases", size=11),
+             Layout(name="findings"),
+         )
+ 
+@@ -205,6 +242,24 @@ class TUIDashboard:
+         stats_table.add_row("Errors / Warnings", f"{snap['errors']} / {snap['warnings']}")
+         layout["stats"].update(Panel(stats_table, title="Statistics", border_style="cyan"))
+ 
++        # Activity panel
++        activity_table = Table(box=box.SIMPLE, show_header=True, padding=(0, 1))
++        activity_table.add_column("Item", style="bold")
++        activity_table.add_column("Value", justify="right")
++        hosts = snap.get("top_hosts", [])
++        if not hosts:
++            activity_table.add_row("Host —", "0")
++        for host, count in hosts[:4]:
++            activity_table.add_row(f"Host {host[:14]}", str(count))
++
++        activity_table.add_section()
++        ports = snap.get("top_ports", [])
++        if not ports:
++            activity_table.add_row("Port —", "0")
++        for port, freq in ports[:4]:
++            activity_table.add_row(f"Port {port}", str(freq))
++        layout["activity"].update(Panel(activity_table, title="Host/Port Activity", border_style="magenta"))
++
+         # Severity breakdown
+         sev = snap["severity_counts"]
+         sev_table = Table(box=box.SIMPLE, show_header=False, padding=(0, 2))
+@@ -216,6 +271,25 @@ class TUIDashboard:
+             sev_table.add_row(Text(s.upper(), style=colors[s]), str(sev.get(s, 0)))
+         layout["severity"].update(Panel(sev_table, title="Findings", border_style="red"))
+ 
++        # Phase lifecycle
++        status_style = {
++            "queued": "dim",
++            "running": "bold cyan",
++            "done": "green",
++            "failed": "red",
++            "skipped": "yellow",
++        }
++        phases_table = Table(box=box.SIMPLE, show_header=True, padding=(0, 1))
++        phases_table.add_column("Phase")
++        phases_table.add_column("Status", justify="right")
++        phase_status = snap.get("phase_status", {})
++        if phase_status:
++            for phase, status in phase_status.items():
++                phases_table.add_row(phase[:18], Text(status.upper(), style=status_style.get(status, "dim")))
++        else:
++            phases_table.add_row("—", "QUEUED")
++        layout["phases"].update(Panel(phases_table, title="Phase Timeline", border_style="blue"))
++
+         # Recent findings
+         findings_table = Table(box=box.SIMPLE, show_header=True, padding=(0, 1))
+         findings_table.add_column("Sev", width=5)
+@@ -233,8 +307,17 @@ class TUIDashboard:
+ 
+         # Footer
+         target = snap.get("current_target", "—")
++        footer_status = (
++            snap.get("last_error")
++            or snap.get("last_warning")
++            or snap.get("last_success")
++            or "No recent alerts"
++        )
++        if len(footer_status) > 70:
++            footer_status = footer_status[:67] + "..."
+         footer_text = Text.assemble(
+             ("  Target: ", "dim"), (target, "bold"),
++            ("  |  ", "dim"), (footer_status, "bold yellow"),
+             ("  |  Press Ctrl+C to stop  ", "dim"),
+         )
+         layout["footer"].update(Panel(footer_text, style="on dark_blue"))
+@@ -260,9 +343,13 @@ class TUILogHandler(logging.Handler):
+             if record.levelno >= logging.ERROR:
+                 with self.state._lock:
+                     self.state.errors += 1
++                self.state.set_last_error(msg)
+             elif record.levelno >= logging.WARNING:
+                 with self.state._lock:
+                     self.state.warnings += 1
++                self.state.set_last_warning(msg)
++            elif "[+]" in msg or "completed successfully" in msg.lower():
++                self.state.set_last_success(msg)
+ 
+             # Detect findings from log messages
+             msg_lower = msg.lower()
